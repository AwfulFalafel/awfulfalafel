<%= form_with(model: [:admin, @catalog_entry]) do |form| %>
	<%= render(partial: "admin/catalog_entries/form", locals: { form: form }) %>
	<div class="mt-2">
		<%= form.submit("Save song", class: "p-2 rounded-md bg-lime-400 text-black") %>
	</div>
<% end %>