<%= form_with(model: [:admin, @schedule_entry]) do |form| %>
	<%= render(partial: "admin/schedule_entries/form", locals: { form: form }) %>
	<div class="mt-2">
		<%= form.submit("Save schedule entry", class: "p-2 rounded-md bg-lime-400 text-black") %>
	</div>
<% end %>